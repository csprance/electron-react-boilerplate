/**
 * Name: Stage Reducer
 * Type: Redux Reducer
 * Description:
 */

import initialState from './state';

export default function stage(state = initialState, action) {
  switch (action.type) {
    default:
      return state;
  }
}
